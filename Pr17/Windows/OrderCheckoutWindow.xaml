<Window x:Class="Pr17.Windows.OrderCheckoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оформление заказа" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выберите дату получения (не более 7 дней от сегодня):" Margin="0,5"/>

        <TextBlock Grid.Row="2" Text="Способ оплаты:" Margin="0,10,0,5"/>
        <ComboBox x:Name="PaymentMethodCombo" Grid.Row="3" VerticalAlignment="Top" Margin="0,5">
            <ComboBoxItem Content="Наличные при получении" IsSelected="True"/>
            <ComboBoxItem Content="Карта при получении"/>
            <ComboBoxItem Content="Онлайн"/>
        </ComboBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Подтвердить" Width="100" Margin="5" Click="ConfirmButton_Click" IsDefault="True"/>
            <Button Content="Отмена" Width="80" Margin="5" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>